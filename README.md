## **This requires JRE 10 to run**

#### **Kindly do the following**

Run `server.bat` to run the Server **(run this first)**
 
Run `client.bat` to run the Client program **(It can run multiple times)**
